{
  "collectionName": "components_site_sections_search_sections",
  "info": {
    "displayName": "search-section",
    "icon": "search"
  },
  "options": {},
  "attributes": {
    "type": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "events",
        "galleries",
        "qnas"
      ]
    },
    "header": {
      "type": "string"
    },
    "listingMode": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "on",
        "after",
        "before"
      ]
    },
    "defaultSortingMode": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "ascending",
        "descending"
      ]
    },
    "sectionID": {
      "type": "string",
      "maxLength": 32
    }
  },
  "config": {}
}
